apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  namespace: project
  name: shared-storage-claim
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 500Mi
  storageClassName: local-storage
